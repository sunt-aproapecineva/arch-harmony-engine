import { createFileRoute } from "@tanstack/react-router";
import { AdminProgress } from "@/pages/admin/AdminProgress";
export const Route = createFileRoute("/admin/progress")({ component: AdminProgress });
