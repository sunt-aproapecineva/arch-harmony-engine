// Conținutul cursului Arhitectura Afacerii · Start.
//
// Metodologie nouă, în construcție. Structura e identică cu a cursului Business,
// dar TOATE id-urile trebuie să înceapă cu prefixul 'st-' (vezi REGULA ID-URILOR
// din src/lib/courses.ts) — altfel răspunsurile elevilor de la Start s-ar suprapune
// peste cele de la Business, care folosesc aceleași chei text în DB.
import { Module, LiveEvent } from '../types';

export const START_MODULES: Module[] = [];

export const START_LIVE_EVENTS: LiveEvent[] = [];
